var materials = [
    'Hydrogen',
    'Helium',
    'oxygen',
    'carbon'
];

materials.map(function(material) {
    return material.length;
});

materials.map((material) => material.length);

